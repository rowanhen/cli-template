import { describe, it, expect } from "bun:test";

// Placeholder — replace with real tests as you build out commands.
describe("cli", () => {
  it("passes", () => {
    expect(true).toBe(true);
  });
});
